# Examples

Example applications demonstrating PurchaseKit integration.

## iOS

Demo iOS app using the PurchaseKit Swift package from `/ios`. Open `PurchaseKitDemo.xcodeproj` in Xcode and run on a simulator or device.

## Rails

Example Rails app with authentication, paywall, and webhook handling. See `rails/README.md` for setup instructions.
